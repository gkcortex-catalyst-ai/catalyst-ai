apiVersion: apps/v1
kind: Deployment
metadata:
  name: catalyst-ai-proxy
  namespace: catalyst-ai-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catalyst-ai-proxy
  template:
    metadata:
      labels:
        app: catalyst-ai-proxy
    spec:
      containers:
        - name: proxy
          image: catalyst-ai-proxy:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: catalyst-ai-proxy-svc
  namespace: catalyst-ai-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: catalyst-ai-proxy